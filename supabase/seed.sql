-- Seed data for local development. Runs after migrations on `supabase db reset`.
-- Production deployments do NOT run this file.

-- Nothing to seed by default. User signs up → trigger creates user_settings → done.
-- If you want a test alert preloaded, you'd insert here with a known user_id.
